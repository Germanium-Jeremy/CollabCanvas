{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    // CommonJS output so both the NestJS API (CJS) and Next.js can consume it.
    "module": "CommonJS",
    "moduleResolution": "Node",
    "outDir": "dist",
    "rootDir": "src"
  },
  "include": ["src"]
}
